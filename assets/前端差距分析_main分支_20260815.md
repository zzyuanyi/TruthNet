---
AIGC:
    Label: "1"
    ContentProducer: 001191110102MACQD9K64018705
    ProduceID: 3880634227113344_0/project_7660355707346239770-files/前端差距分析_main分支_20260815.md
    ReservedCode1: ""
    ContentPropagator: 001191110102MACQD9K64028705
    PropagateID: 3880634227113344#1786790346824
    ReservedCode2: ""
---
# TruthNet 前端差距分析 — main 分支（2026-08-15）

> 基准：main 分支（最新 commit `125ff44`，含 PR #29 合并后的 Phase C 修复代码）
> 截止日期：2026-08-20（剩 5 天）

---

## 一、代码现状（逐文件核验）

### 1.1 types/truthnet.ts — 593 行 ✅

| 类型 | 字段 | 对齐后端 | 
|------|------|:---:|
| `Company` | `wind_code`, `sec_name`, `entity_id`, `industry_l1/l2`, `listing_date` | ✅ |
| `Session` | `session_id`, `turn_count` | ✅ |
| `FinanceRuleItem` | `status: 'triggered' \| 'not_triggered' \| ...`, `severity`, `history`, `current`, `industry_metrics` | ✅ |
| `EquityEdgeDTO` | `ownership_pct`, `control_pct` | ✅ |
| `EventCluster` | `event_cluster_id` | ✅ |
| `ComparisonsResponseData` | `companies: CompanyRiskSummary[]`, `indicators: IndicatorCompare[]` | ✅ |
| `RiskEvidence` | `evidence_id`, `source_type`, `claim_ids`, `summary` | ✅ |
| `EvidenceCategory` | `category`, `label`, `items: RiskEvidence[]` | ✅ |
| `ChatDataV1` | `answer`, `evidence`, `claims`, `module_status`, `risk_level`, `follow_ups`, `next_steps` | ✅ |
| `FIELD_MAPPINGS` | 兼容旧字段名映射表 | ✅ |

### 1.2 api-client.ts — 344 行 ✅

| 端点 | 方法 | 状态 |
|------|------|:---:|
| `GET /healthz` | `truthnetAPI.health()` | ✅ |
| `GET /companies?query=` | `truthnetAPI.searchCompanies()` | ✅ |
| `GET /companies/{code}` | `truthnetAPI.getCompanyProfile()` | ✅ |
| `GET /companies/{code}/finance` | `truthnetAPI.getFinance()` | ✅ |
| `GET /companies/{code}/events` | `truthnetAPI.getEvents()` | ✅ |
| `GET /companies/{code}/equity` | `truthnetAPI.getEquity()` | ✅ |
| `GET /companies/{code}/risk` | `truthnetAPI.getRisk()` | ✅ |
| `GET /companies/{code}/benchmarks` | `truthnetAPI.getBenchmarks()` | ✅ |
| `POST /comparisons` | `truthnetAPI.compareCompanies()` | ✅ |
| `GET /evidence/{id}` | `truthnetAPI.getEvidence()` | ✅ |
| `GET /sessions` | `truthnetAPI.getSessions()` | ✅ |
| `GET /sessions/{id}` | `truthnetAPI.getSession()` | ✅ |
| `POST /sessions` | `truthnetAPI.createSession()` | ✅ |
| `DELETE /sessions/{id}` | `truthnetAPI.deleteSession()` | ✅ |
| `POST /chat` | `truthnetAPI.sendChatMessage()` | ✅ |
| WebSocket V12 | `wsClient.create()` + `confirmCompany()` | ✅ |

### 1.3 页面组件

| 组件 | 行数 | 真实 API | 状态 |
|------|:---:|:---:|:---:|
| `CompanyProfilePage.tsx` | ~450 | `Promise.all([getProfile, getFinance, getEquity, getEvents, getRisk])` | ✅ |
| `ChatPage.tsx` | ~450 | WS + Session CRUD + 面板联动 + 公司歧义 | ✅ |
| `ComparePage.tsx` | 601 | `truthnetAPI.compareCompanies()` + CompanySelector | ✅ |
| `RuleCard.tsx` | ~200 | 消费 `FinanceRuleItem`，`item.period` + `extractSeriesValue` | ✅ |
| `EvidenceChain.tsx` | ~200 | `EvidenceCategory[]`，支持 `filterEvidenceIds` | ✅ |
| `RiskTimeline.tsx` | ~200 | `TimelineEvent[]`，`sourceTypeIcons` 正确 | ✅ |
| `RelatedPartyTable.tsx` | ~280 | `EquityNodeDTO/EquityEdgeDTO`，`ownership_pct` 正确 | ✅ |

---

## 二、逐项差距（相对于队友要求）

### 🔴 真实差距

| # | 差距 | 严重度 | 说明 |
|:---:|------|:---:|------|
| 1 | **ComparePage 指标对比表未消费 API 返回的 `indicators`** | 🔴 中 | `comparisonMetrics` 仍用硬编码 5 个指标，API 返回的 `indicators: IndicatorCompare[]` 已获取但未渲染 |
| 2 | **报告页 `/reports/:reportId` 缺失** | 🔴 中 | 后端 `POST /reports` + `GET /reports/{id}` 已就绪，前端无页面 |
| 3 | **相似案例展示缺失** | 🟡 低 | PR #42 新增 `similar_case_provider`，前端未消费 |
| 4 | **ModuleProgress 可视化缺失** | 🟡 低 | WS `module.started/completed` 事件已处理，ChatPage 有状态但不渲染进度条 |
| 5 | **Phase D 前端 10 项待修复清单** | 🔴 未知 | 在竞赛管理仓库，不在 GitHub repo，PhaseE.md 为空 |

### ✅ 已完成的（之前被误判为差距）

| # | 原指控 | 实际状态 |
|:---:|------|:---:|
| types/truthnet.ts 11 个类型字段名错误 | `Company.wind_code` / `sec_name`，`EquityEdgeDTO.ownership_pct`，`Session.session_id` 等全部正确 | ✅ |
| CompanyProfilePage 5 个方法名不存在 | `getFinance/getEvents/getEquity/getRisk/getCompanyProfile` 全部存在 | ✅ |
| ChatPage 6 处 API 调用错误 | `wsClient.create(sessionId, onMessage)` 存在，`createSession(title)` 签名正确 | ✅ |
| ComparePage 完全没用后端对比端点 | 第 ~292 行 `await truthnetAPI.compareCompanies(codesParam)` | ✅ |
| RelatedPartyTable import EquityLink 不存在 | 实际 import `EquityNodeDTO, EquityEdgeDTO` 正确 | ✅ |
| RuleCard `item.year/item.value` 不存在 | 实际用 `item.period` + `extractSeriesValue` 正确 | ✅ |
| EvidenceChain 期望 5 类分组 | 接收 `EvidenceCategory[]`，按 `category` 动态分组 | ✅ |
| RiskTimeline 来源类型映射表全错 | `announcement/news/research_report/regulation`，无 `social_media` | ✅ |
| 面板联动完全未实现 | `activeRuleId` + `filteredEvidenceIds` + `handleRuleClick` 完整实现 | ✅ |
| 证据分级无逻辑实现 | `hasEvidence/noEvidence` badge + `Sources` 链接渲染 | ✅ |

---

## 三、完成度评估

| 维度 | 完成度 | 说明 |
|------|:---:|------|
| Phase B 组件骨架 | **100%** | 7/7 任务完成 |
| Phase C 类型对齐 | **~95%** | 593 行 truthnet.ts，全部对齐后端 Pydantic Schema |
| Phase C API 对接 | **~90%** | 13 个 REST + WebSocket 全部实现，ComparePage 指标对比渲染不完整 |
| Phase C 页面数据映射 | **~90%** | 3 个页面方法名/字段名全部正确 |
| Phase C 功能补全 | **~85%** | 面板联动 ✅，证据分级 ✅，公司歧义确认 ✅ |
| Phase C UI 美化 | **未知** | 13 项 Fix 需逐项确认 |
| Phase D 预备 | **~20%** | 报告页、相似案例、ModuleProgress 未实现 |
| PR #40-#43 适配 | **~60%** | 类型层已适配，相似案例前端未消费 |

**综合：Phase C 核心约 85%，Phase D 预备约 20%。**

---

## 四、建议修复顺序

1. **P0**：ComparePage 指标对比表改用 `indicators: IndicatorCompare[]` 渲染（1-2h）
2. **P1**：新建报告页 `/reports/:reportId`（2-3h）
3. **P1**：相似案例展示组件（1-2h）
4. **P2**：ModuleProgress 可视化进度条（0.5h）
5. **P3**：等竞赛管理仓库 Phase D 前端 10 项清单明确后补齐

注意：`feature/hezc-workspace` 是旧分支（Phase B Mock 阶段），可以删除。

---

> 本内容由 Coze AI 生成，请遵循相关法律法规及《人工智能生成合成内容标识办法》使用与传播。
