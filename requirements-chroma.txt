# TruthNet Chroma 嵌入链路依赖
# Phase C 开始时安装: pip install -r requirements-chroma.txt
# 不包含在 requirements.txt 中，避免 Phase B 全员下载 ~2GB PyTorch
-r requirements.txt

sentence-transformers==3.2.1
transformers==4.45.2
tokenizers==0.20.3
torch==2.4.1
modelscope==1.39.0
# huggingface 国内不可达时，modelscope 自动镜像下载 BGE 模型
# pip install --default-timeout 180 --retries 10 -i https://pypi.tuna.tsinghua.edu.cn/simple modelscope
